---
name: coursehub
description: >-
  从课程 PPT / Syllabus 生成单文件课程复习网站（多周分页、全局/分页/词表三级搜索、
  复习清单、课堂笔记、词汇速查、每页展开收起、移动端适配），并可部署到 Cloudflare
  实现免费上线与跨设备数据同步；支持每周增量更新。当用户提供课程 PPT / 讲义 / Syllabus
  并希望「生成课程复习网站」「更新到 Week X」「把课件变成复习站」时使用。
license: MIT
---

# CourseHub

把任意课程的课件变成**结构完整、内容详实、免费上线、跨设备同步**的个人复习网站。
单文件 HTML，无任何外部依赖，任何 AI 按本 Skill 工作流执行都能产出同款成品。

## 什么时候用

- 用户上传课程 PPT / PDF / 讲义 / Syllabus，希望生成复习网站；
- 用户说「更新到 Week X」，希望在已有网站上增量添加新一周内容；
- 用户希望网站部署上线、或加上跨设备同步。

## 输入要求

- 课程名称（英文全称 + 中文译名）；
- Syllabus / 课程大纲（可选）；
- 每周 PPT / PDF / 讲义（可能含大量图片页，需要逐页读取 OCR，不要跳页）。

## 工作流程

1. **读取**：先完整读取 Syllabus 与 PPT。PPT 是图片型 PDF 时逐页 OCR，
   不用「印象」代替实际内容（见 references/main-prompt.md 第 1 步）。
2. **规划**：确定页面列表与每个 Week 的知识点清单；先想清楚每个知识点
   「怎么解释、怎么举例、怎么排序」，再写 HTML。
3. **生成**：按 references/main-prompt.md 的「结构规范 / 样式规范 / 内容写作规范 /
   功能清单」产出完整单文件 HTML（首次建站）或增量更新（每周更新）。
4. **自查**：按 main-prompt.md 末尾「校验清单」逐项核对，重点：
   每个知识点都有 定义 + 通俗解释 + 例子 + 记忆点；默认落地页是 Syllabus；
   桌面三栏与移动单列都正常；刷新后手动添加的数据仍在。
5. **部署**：用户要求发布时按 references/deploy-cloudflare.md 部署；
   数据同步按该文档的「版本号方向判断」逻辑实现（不要用纯覆盖或纯合并）。

## 引用文件

| 文件 | 用途 |
|---|---|
| `references/main-prompt.md` | 首次建站主提示词：结构/样式/内容/功能/校验清单 |
| `references/weekly-update-prompt.md` | 每周增量更新提示词 |
| `references/deploy-cloudflare.md` | 免费部署 + 跨设备同步（Worker + KV）完整方案与踩坑 |
| `assets/example.html` | 一份完整可运行的示例成品（课程复习网站） |

## 输出规范

- 产物始终是**单个 .html 文件**：CSS/JS 全部内联，不引用任何外部资源
  （无图片/视频/CDN/字体/框架），保证离线可用、任何设备能打开。
- 内容中英对照：标题「英文术语（中文）」，正文中文，核心术语绿色加粗。
- 知识点务必详细：定义 → 通俗解释 → 例子 → 记忆点，缺一不可。
- 桌面端布局与移动端适配（`@media(max-width:700px)`）隔离，互不影响。
