---
name: coursehub
description: >-
  从课程 PPT / Syllabus 生成单文件课程复习网站（按课程单元分页、全局/分页/词表三级搜索、
  复习清单、课堂笔记、词汇速查、富文本编辑、日程安排日历、课堂图片上传、每页展开收起、
  主题色可选、移动端适配），并可部署到 Cloudflare（Worker + KV + R2）实现免费上线与
  跨设备数据同步；支持按课程单元增量更新。时间线/分类单元不固定为 Week，
  而是依据使用者 PPT 标题与用语自动推断（Week / Lecture / Unit / Chapter / Class / Module 等）。
  当用户提供课程 PPT / 讲义 / Syllabus 并希望「生成课程复习网站」「更新到新一单元」「把课件变成复习站」时使用。
license: Non-Commercial
---

# CourseHub

把任意课程的课件变成**结构完整、内容详实、免费上线、跨设备同步**的个人复习网站。
单文件 HTML，无任何外部依赖，任何 AI 按本 Skill 工作流执行都能产出同款成品。

## 什么时候用

- 用户上传课程 PPT / PDF / 讲义 / Syllabus，希望生成复习网站；
- 用户说「更新到新一单元」（Lecture X / Unit X / Chapter X / Week X 等），希望在已有网站上增量添加新内容；
- 用户希望网站部署上线、或加上跨设备同步（复习清单 / 单词 / 笔记 / 编辑 / 日历 / 图片全同步）。

## 输入要求

- 课程名称（英文全称 + 中文译名）；
- Syllabus / 课程大纲（可选，用于生成「关键信息速览」+ 预置日历节点）；
- 课程 PPT / PDF / 讲义（按单元提供，可能含大量图片页，需要逐页读取 OCR，不要跳页）。

## 工作流程

0. **询问主题色（生成前必做）**：先问使用者「想要什么主题色？直接回车用默认绿色，
   或输入蓝色/红色/紫色/橙色等，也可以输入任意十六进制色值（如 #2563eb）」。若使用者选择，
   整站所有原本的绿色（主色/深色/亮色/浅底/框文字/说明文字各档）统一替换为所选颜色的同色系
   色板；若不选，默认绿色（#22c55e 系）。后续每次更新保持同一主题色不变。
1. **读取与推断单元**：先完整读取 Syllabus 与 PPT。PPT 是图片型 PDF 时逐页 OCR，
   不用「印象」代替实际内容（见 references/main-prompt.md 第 1 步）。
   读取后根据 PPT 标题（如 Lecture 3 / Unit 2 / Chapter 5 / Week 3）、页眉或使用者的用语，
   **推断本门课的时间线/分类单元类型**：Week / Lecture / Class / Unit / Chapter / Module / Topic 等，
   整站统一使用该单元命名（如 Lecture 1..N），**不要硬编码成 Week**。
2. **规划**：确定页面列表与每个单元的知识点清单；先想清楚每个知识点
   「怎么解释、怎么举例、怎么排序」，再写 HTML。
3. **生成**：按 references/main-prompt.md 的「结构规范 / 样式规范 / 内容写作规范 /
   功能清单」产出完整单文件 HTML（首次建站）或增量更新（新单元）。
   必须包含以下完整功能（一个都不能少）：
   - 三级搜索（全局精准命中 + 关键词主题色高亮；每页局部搜索；词汇速查搜索）；
   - 复习清单（多选一键批量加入、清单内直接展开详情、Know 移出）；
   - 课堂笔记（随时记录、可编辑、可删除）；
   - 词汇速查（可手动添加中英单词、可删除）；
   - **富文本编辑**：知识点 / 手动添加的知识点 / 课堂笔记都可进入编辑态，工具栏含
     加粗 / 斜体 / 下划线 / 删除线 / 高亮三色（黄绿粉）/ 字色两色（蓝黄）/
     有序列表 / 无序列表 / 五种彩框（灰绿蓝红黄），编辑后保存并云端同步；
   - **日程安排日历**：左侧目录有「📅 日程安排」入口，点击后中间主区展示大日历；
     Syllabus 中的 quiz / assignment / 考试 / 阅读周等关键日期**预置标注**（颜色分级：
     红=重要 橙=提醒 蓝=一般 绿=阅读 紫=考试）；使用者可自行添加 / 删除事件、自定义颜色；
   - **课堂图片上传**：每个单元页底部「其他知识点」区下方，可拖拽或点击上传课堂照片，
     前端压缩（最长边 1920px / JPEG 0.82）后存云端 + 本地缓存，跨设备同步，可删除；
   - 每页「全部展开/收起」、每页底部「其他知识点」手动补充区、使用说明悬浮 icon、
     左侧目录课程单元折叠组、移动端适配。
4. **自查**：按 main-prompt.md 末尾「校验清单」逐项核对，重点：
   每个知识点都有 定义 + 通俗解释 + 例子 + 记忆点；默认落地页是 Syllabus；
   单元命名全站一致且与 PPT 标题吻合（PPT 是 Lecture 就不要叫 Week）；
   搜索必须「精准匹配知识点 + 点击直达 + 关键词主题色高亮」；桌面三栏与移动单列都正常；
   富文本（字色/高亮/列表/彩框）、日历增删、图片上传删除在浏览器实测可用；
   刷新后手动添加的数据仍在。
5. **部署确认（必做，先问再做）**：生成并自查通过后，**必须主动询问使用者**：
   「需要我帮你免费部署上线、并开启跨设备同步吗？（推荐：手机/电脑/任何浏览器打开同一链接，数据同步）」
   - 使用者**需要** → 按 references/deploy-cloudflare.md 实现「Worker + KV + R2」云端同步
     （前端 pushToCloud / loadFromCloud + 版本号方向判断；结构化数据走 KV，
     图片走 R2）并部署，交付线上链接；
   - 使用者**不需要 / 只想本地用** → 明确告知后果：不部署时，网站是纯本地文件，
     「复习清单 / 手动添加的单词 / 其他知识点 / 课堂笔记 / 编辑 / 日历 / 图片」只保存在
     当前浏览器的 localStorage / IndexedDB 里，**换设备、换浏览器、清缓存都会丢失或不同步**；
     需要跨设备同步时随时可以再部署。把这段提醒完整说给使用者，不能省略。

## 引用文件

| 文件 | 用途 |
|---|---|
| `references/main-prompt.md` | 首次建站主提示词：结构/样式/内容/功能（含富文本、日历、图片）/校验清单 |
| `references/weekly-update-prompt.md` | 新增单元更新提示词（每次新课 / 新 PPT 时用） |
| `references/deploy-cloudflare.md` | 免费部署 + 跨设备同步（Worker + KV + R2）完整方案与踩坑 |
| `assets/example.html` | 一份完整可运行的示例成品（课程复习网站，含全部功能） |

## 输出规范

- 产物始终是**单个 .html 文件**：CSS/JS 全部内联，不引用任何外部资源
  （无图片/视频/CDN/字体/框架），保证离线可用、任何设备能打开。
- 内容中英对照：标题「英文术语（中文）」，正文中文，核心术语主题色加粗。
- 知识点务必详细：定义 → 通俗解释 → 例子 → 记忆点，缺一不可。
- 桌面端布局与移动端适配（`@media(max-width:700px)`）隔离，互不影响。
